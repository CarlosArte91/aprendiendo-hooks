/**
 * En este ejemplo se practican los parametros que se pasan a un componente hijo
 * y la ejecución de un método desde el hijo hacia el padre para actualizar el estado
 * También se utiliza el
 */
function FatherComponent() {};

export default FatherComponent;
